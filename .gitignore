# build tooling deps (run `npm install` inside build/ to restore)
build/node_modules/
node_modules/

# QA screenshot artifacts
build/shots/

# CV PDFs are generated in CI (deploy workflow) from js/profile.js
cv/*.pdf

# misc
.DS_Store
*.log
_site/
